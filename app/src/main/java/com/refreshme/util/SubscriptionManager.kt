package com.refreshme.util

/**
 * Subscription system removed — RefreshMe now earns via a 10% platform fee on bookings.
 * This stub always returns true so existing references compile without changes.
 */
object SubscriptionManager {
    @Suppress("UnusedParameter")
    suspend fun isSubscriptionActive(): Boolean = true
}