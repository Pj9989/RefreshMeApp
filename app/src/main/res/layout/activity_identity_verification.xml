<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fillViewport="true"
    android:background="@color/black">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center"
        android:padding="32dp">

        <!-- Shield / identity icon -->
        <ImageView
            android:layout_width="96dp"
            android:layout_height="96dp"
            android:layout_marginBottom="24dp"
            android:src="@android:drawable/ic_lock_lock"
            android:contentDescription="Identity verification icon"
            app:tint="@color/gold" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Identity Verification"
            android:textSize="24sp"
            android:textStyle="bold"
            android:textColor="@android:color/white"
            android:layout_marginBottom="16dp" />

        <!-- Dynamic status label -->
        <TextView
            android:id="@+id/verificationStatusText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Not Verified"
            android:textSize="18sp"
            android:textColor="@color/gold"
            android:textStyle="bold"
            android:layout_marginBottom="12dp" />

        <!-- Dynamic description -->
        <TextView
            android:id="@+id/verificationDescriptionText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Verify your identity to unlock premium features."
            android:textSize="14sp"
            android:textColor="#CCCCCC"
            android:textAlignment="center"
            android:lineSpacingMultiplier="1.4"
            android:layout_marginBottom="32dp" />

        <!-- Progress indicator shown while calling the Firebase Function -->
        <ProgressBar
            android:id="@+id/verificationProgressBar"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:visibility="gone"
            android:layout_marginBottom="16dp" />

        <!-- CTA button -->
        <Button
            android:id="@+id/startVerificationButton"
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:text="Verify My Identity"
            android:textSize="16sp"
            android:backgroundTint="@color/gold"
            android:textColor="@color/black"
            app:cornerRadius="12dp" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Your documents are processed securely by Stripe. RefreshMe does not store your ID."
            android:textSize="12sp"
            android:textColor="#888888"
            android:textAlignment="center"
            android:layout_marginTop="24dp"
            android:lineSpacingMultiplier="1.3" />

    </LinearLayout>
</ScrollView>
